import 'package:flutter/material.dart';

class BedtimePage extends StatelessWidget {
  const BedtimePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Set time for bed')));
  }
}
